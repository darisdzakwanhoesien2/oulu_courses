Your screenshot shows a textured cube, which means you've successfully completed several key steps in the assignment:

Drawing the Cube: You've defined the vertices for the cube.
Texturing: You've mapped the texture onto the cube.
Here’s what you still need to do to complete the assignment:

1. Camera Movement and Inputs

Ensure that the camera controls are fully functional by completing the mvp_controller.py file. This will allow you to move around the scene and debug the texture mapping.

2. Loading External Objects

Use the provided ObjLoader and TextureLoader utilities to load an external object (e.g., a .obj file and its corresponding texture).
Complete the init_context_load function by referencing the logic in init_context_raw.
Uncomment the line in the main function to call win.init_context_load instead of win.init_context_raw.
Run the code and take a screenshot of the loaded object for your report.

3. Shader Programming

Modify the vertex.glsl and fragment.glsl files to implement transformations and texture adjustments.

Vertex Shader: For example, scale the cube by multiplying the position_offset with a scale factor (e.g., 0.5).
Fragment Shader: Adjust the texture color, such as inverting the colors.

Take a screenshot of the changes you made using the shaders.

4. Report and Submission

Screenshots: Ensure you have screenshots for:

The basic cube (already done).
The cube with a single triangle textured.
The fully textured cube (already done).
The loaded external object.
The shader-modified cube.

Your screenshot shows a textured cube, which means you've successfully completed several key steps in the assignment:

Drawing the Cube: You've defined the vertices for the cube.
Texturing: You've mapped the texture onto the cube.
Here’s what you still need to do to complete the assignment:

1. Camera Movement and Inputs

Ensure that the camera controls are fully functional by completing the mvp_controller.py file. This will allow you to move around the scene and debug the texture mapping.

2. Loading External Objects

Use the provided ObjLoader and TextureLoader utilities to load an external object (e.g., a .obj file and its corresponding texture).
Complete the init_context_load function by referencing the logic in init_context_raw.
Uncomment the line in the main function to call win.init_context_load instead of win.init_context_raw.
Run the code and take a screenshot of the loaded object for your report.

3. Shader Programming

Modify the vertex.glsl and fragment.glsl files to implement transformations and texture adjustments.

Vertex Shader: For example, scale the cube by multiplying the position_offset with a scale factor (e.g., 0.5).
Fragment Shader: Adjust the texture color, such as inverting the colors.

Take a screenshot of the changes you made using the shaders.

4. Report and Submission

Screenshots: Ensure you have screenshots for:

The basic cube (already done).
The cube with a single triangle textured.
The fully textured cube (already done).
The loaded external object.
The shader-modified cube.


